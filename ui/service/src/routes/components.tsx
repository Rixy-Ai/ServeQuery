import { CreateRouterLinkComponent } from 'servequery-ui-lib/router-utils/components/navigations'
import type { Routes } from 'routes/types'

export const RouterLink = CreateRouterLinkComponent<Routes>()
